import sqlite3

conn = sqlite3.connect("analytics.db")
conn.execute("CREATE TABLE IF NOT EXISTS sales (id INTEGER, amount REAL);")
conn.execute("INSERT INTO sales VALUES (1, 100.0), (2, 150.0), (3, 200.0);")
conn.commit()
conn.close()
